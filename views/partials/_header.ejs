<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/login">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <% if (user) { %>
        <div style="margin-left: auto; margin-right: 0">Logged in as: <%= user.email %></div>
        <div style="margin-left: 20px; margin-right: 0">
          <form action="/logout" method="POST">
            <button type="submit" class="btn btn-outline-dark">Logout</button>
          </form>
        </div>
      <% } else{ %>
        <div style="margin-left: auto; margin-right: 0">
          <form class="form-inline" action="/login" method="GET">  <!-- EDIT form -->
            <div class="form-group mb-2">
              <button type="submit" class="btn btn-outline-dark" style="margin-left: 10px">Login</button>
            </div>
          </form>
        </div>
        <div>
          <form class="form-inline" action="/register" method="GET">  <!-- EDIT form -->
            <div class="form-group mb-2">
              <button type="submit" class="btn btn-outline-dark" style="margin-left: 10px">Register</button>
            </div>
          </form>
        </div>
        <% } %>
    </div>
  </nav>
</header>